appId: ${APP_ID}
name: Restore an authenticated session after relaunch
tags:
  - auth
  - critical
---
- launchApp:
    clearState: true
- runScript:
    file: ../../scripts/prepare-auth-user.js
    env:
      CREATE_VIA_API: 'true'
- runFlow:
    file: ../../subflows/login.yaml
    env:
      EMAIL: ${output.auth.email}
      PASSWORD: ${output.auth.password}
- assertVisible:
    id: ${TEST_ID_DASHBOARD_SCREEN}
- stopApp
- launchApp
- assertVisible:
    id: ${TEST_ID_DASHBOARD_SCREEN}
